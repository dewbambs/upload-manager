name: upload_manager
version: 0.1.0
publish_to: 'none'
environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.30.0
  firebase_auth: ^4.17.5
  firebase_storage: ^12.1.5
  workmanager: ^0.5.1
  file_picker: ^8.0.1
  path_provider: ^2.1.2

flutter:
  uses-material-design: true
